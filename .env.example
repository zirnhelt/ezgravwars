# Production API URL (Cloudflare Worker)
# Get this URL after deploying the worker with: npm run worker:deploy
VITE_API_URL=https://gravity-wars-api.YOUR_SUBDOMAIN.workers.dev

# For local development, this defaults to http://localhost:8787
# See src/config.js for the fallback logic
